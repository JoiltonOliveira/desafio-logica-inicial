# Desafio DIO – Classificador de Nível de Herói

Este projeto é um exercício em JavaScript que classifica o nível de um herói com base em sua experiência (XP) e exibe uma mensagem específica de personagem usando `switch`.

---

## 🚀 Como rodar

1. Abra o terminal na pasta do projeto.
2. Execute:

```bash
node index.js
```

---

## ✅ O que está implementado

- Uso de objetos para representar um herói
- Uso de condicionais `if / else if / else` para determinar o nível a partir do XP
- Uso de `switch / case / default` para exibir uma mensagem com base no nome do herói
- Saída de texto no console

---

## 🛠️ Como testar / alterar

No arquivo `index.js`, altere estas duas propriedades do objeto `pirataDoChapeuDePalha` para testar diferentes resultados:

- `nome`: nome do personagem (por exemplo, `"Usopp"`, `"Nami"`, `"Roronoa Zoro"`)
- `nivelDePirata`: experiência (XP) usada para classificar o nível

---

## 🧠 O que você aprende com este projeto

- Calcular faixas de valores usando condicionais
- Usar `switch` para decidir entre vários casos
- Praticar lógica simples em JavaScript

---

*Divirta-se construindo e personalizando!*